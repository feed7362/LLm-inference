[
  {
    "name": "calculator",
    "description": "Evaluate a mathematical expression using safe Python functions and math module.",
    "arguments": {
      "type": "object",
      "properties": {
        "expression": {
          "type": "string",
          "description": "Mathematical expression to evaluate, e.g., 'sin(pi/2) + log(10)'"
        }
      },
      "required": ["expression"]
    }
  },
  {
    "name": "datetime_now",
    "description": "Returns the current date and time.",
    "arguments": {
      "type": "object",
      "properties": {
        "format": {
          "type": "string",
          "description": "Returns the current date and time."
        }
      }
    }
  }
]
